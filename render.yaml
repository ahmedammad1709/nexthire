services:
  - type: web
    name: nexthire-ai-backend
    env: python
    branch: main
    region: oregon
    runtime: python-3.11.9
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT --timeout 60 app:app
